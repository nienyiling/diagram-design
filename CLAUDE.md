# 公務用圖表工具

純靜態、純前端。主體是**九種填表產生器**（流程圖、泳道圖、組織圖、關係圖、家系圖、甘特圖、時間軸、
分層堆疊圖、四象限），流程圖另外有一種**畫板**畫法（`#/board`），另附 153 張現成範本可改字
（首頁預設收在 `<details>` 裡）。使用者是公務同仁，要做的是簡報與公文裡那張圖。
**「資料不離開這台裝置」是這個站存在的理由**，任何修改都不得違反。

**產生器不靠 AI，也不准靠。** 使用者填表時結構就已經給定了（哪一列是判斷、哪一項幾號開始），
剩下的只是算術。要 AI 的是「從一段自由文字猜出結構」——本站不做那件事。

## 鐵律

1. **零對外連線**。不加 CDN、不加後端、不加追蹤、不呼叫 LLM、不載 webfont。
   e2e 有一項專門攔 `page.on('request')` 驗證整趟操作沒有任何外部請求，不要把它拿掉。
2. **來源標註不可拿掉**。範本取自 `cathrynlavery/diagram-design`（MIT，Cathryn Lavery），
   首頁說明卡、以及每一份「從範本來的」下載檔（SVG／PNG／HTML／Word）都要帶上來源與授權。
   **反過來也一樣重要：自己畫的圖不要掛那個來源。** 關係圖與家系圖在上游那 153 張裡
   沒有對應的範本，掛上去等於引用了一個跟這張圖無關的出處。判準是 descriptor 有沒有
   `sample`（`forms.js` 傳 `credit: !!gen.sample`），e2e 兩邊各有一項盯著。
   字串集中在 `core.js` 的 `SOURCE`，`core.unit` 有一組測試盯著它，不要各處各寫一份。
   `vendor/upstream/` 裡的 LICENSE 與 SOURCE.json 不可刪。
3. **純函式不碰 DOM**。純函式層是 `app/core.js`、`app/gen.js`、`app/board.js`
   （後兩者只相依 core.js），Node 直接 require 得動；
   `app/app.js`／`app/editor.js`／`app/forms.js`／`app/canvas.js` 才碰 DOM 與下載。
   混在一起就測不了。
4. **`data/diagrams.json` 是建置產物，但要 commit**。部署時直接送上 CDN，不在 CDN 上跑建置。
   改了 `core.js` 的轉換規則就要 `npm run build`，忘了跑 `tests/data.mjs` 會直接紅。

## 架構

- `index.html`：介面與樣式。視覺刻意跟公務用工具箱（gongwu-calc）對齊：暖紙底 `#f3f1e9`、
  印章紅 `#9d2b25`、標題襯線體、卡片白底暖灰框。**字型只寫字族名、絕不載 webfont。**
- `app/core.js`：抽 SVG、收 CSS、換字型、換色、折行、組匯出檔、分類與篩選。
  UMD 包法，瀏覽器掛 `window.DD`，Node `require` 同一份。零相依。
- `app/gen.js`：**九種產生器的純函式層**，外加「從 Excel 貼一整塊」與產生器的設定檔。
  掛 `window.DDGen`，Node `require` 同一份。
  每一種是一個 descriptor：`{id, name, use, rowName, sample, sampleTitle, help, fields, meta, example, build(rows, meta, opts)}`。
  `fields`／`meta` 是**畫面照著長出表單的規格**，不是註解——新增一種圖只要在這裡加一個
  descriptor，`forms.js` 不必動。
- `app/app.js`：載 `data/diagrams.json`、畫做圖入口與範本列表、hash 路由
  （`#/` 首頁、`#/make/<類型>` 產生器、`#/範本id` 改字；舊的 `#/flow` 轉到 `#/make/flow`）。
- `app/editor.js`：預覽與匯出。掛 `window.DDEditor`。
- `app/board.js`：**自由畫板的純函式層**。掛 `window.DDBoard`，只相依 core.js。
  形狀清單 → SVG、連線繞路、命中測試、縮放、設定檔。
- `app/forms.js`：產生器的畫面層。掛 `window.DDForms`。
- `app/canvas.js`：畫板的畫面層（指標事件）。掛 `window.DDCanvas`。
- `scripts/build-data.mjs`：`vendor/upstream/*.html` ＋ `content/zh-samples.json` → `data/diagrams.json`。
- `content/zh-samples.json`：人工寫的公務情境中文範例，一張圖一筆，`texts` 依序號對應圖上每一段。
  用 `node scripts/dump-segments.mjs <範本id>` 查序號。**只在建置時用，不部署。**
- `vendor/upstream/`：上游範例圖的原樣副本（153 個 HTML）＋ LICENSE ＋ SOURCE.json。
  只在建置時用，**不部署**（部署包只有 `index.html`、`_headers`、`app/`、`data/`）。

### 幾個踩過的坑

- **`<svg>` 的抽取要「第一個 `<svg` 到最後一個 `</svg>`」**。上游有 9 張圖在主 SVG 裡放了
  巢狀的小 svg 當圖示，用非貪婪比對會在第一個內層圖示就收尾，整張圖只剩前幾百個位元組——
  而且畫面上看起來只是「這張圖怪怪的」，不會報錯。
- **收進 SVG 的 CSS 一定要冠上那張圖自己的 class**。`<style>` 是整份文件共用的，
  首頁一次排 150 張圖，不冠的話 A 圖的 `.node{fill:...}` 會把 B 圖的 `.node` 一起改掉，
  而且只在「兩張圖同時出現在畫面上」時才看得出來。`@keyframes` 也要改名，同理。
- **`:root` 不能原封不動搬進 SVG**。inline 在 HTML 裡時 `:root` 指的是 `<html>`，
  會把整站的設計語彙變數蓋掉。一律改寫成 `.dd-<id>`。
- **字型藏在三個地方**：SVG 的 `font-family` 屬性、CSS 的 `font-family` 宣告，
  還有 `--sans`／`--serif`／`--mono`／`--font-sans` 這類自訂屬性。
  漏掉自訂屬性那一種，整張圖看起來沒事，實際上還在指名要 Geist——中文就變成方塊或退回預設字。
- **換色要先換成暫時色再換成目標色**。`ink→paper`、`paper→ink` 這種對調直接換的話，
  第二輪會把第一輪剛換好的顏色再換一次，整張圖只剩一個顏色。
- **巢狀 `<svg>` 一定要明寫 `x`／`y`／`width`／`height`**。只給 viewBox 的話它會撐滿整個外框，
  直接蓋掉上面剛加的標題。
- **量文字寬度前要先讓元素看得見**。`getComputedTextLength()` 在 `display:none` 底下一律回 0，
  自動縮字就整個失效。路由要先把 `#editorView` 顯示出來，再呼叫 `DDEditor.open()`。
- **字級要換算回 SVG 的座標單位**。`getComputedStyle().fontSize` 給的是畫面上的 px，
  SVG 被縮放過，直接拿去設會整個跑掉。除以「畫面寬 ÷ viewBox 寬」才是使用者單位。
  另外用 inline style 設而不是設屬性——CSS class 的規則會蓋掉屬性。
- **折行時行首的空白要丟掉**。留著會佔掉下一行的寬度，連鎖起來每一行都少一個字，
  最後一個字被擠成單獨一行（實測 `alpha beta gamma` 折成 `alpha / beta / gamm / a`）。
- **上游有三張深色範例把滿版底色寫死成淺色**（長條圖、甘特圖、散布圖），
  白底配白網點幾乎看不見。`fixDarkBackdrop()` 只換那一塊滿版的底，
  其餘的 `#f5f5f5` 在深色圖裡是文字色，不能一起換。
- **`<foreignObject>` 不會被畫進 PNG**。三張分層資料架構圖用到它，
  建置時就標成 `png:false`，畫面上直接停用 PNG 按鈕並說明原因。悄悄產出一張缺一塊的圖更糟。
- **`package.json` 不要加 `"type": "module"`**。加了之後 `app/core.js` 會被 Node 當成 ESM，
  UMD 的 `module.exports` 那一支就走不到，`require()` 拿回來的是空的。測試檔本來就是 `.mjs`，
  不需要那個欄位。
- **無頭 Chromium 在容器裡會把非 ASCII 的下載檔名整個丟掉**（`測試檔.txt` → `download`）。
  那是測試環境的語系問題，不是站上的問題；e2e 的檔名斷言改用英文標題，
  中文檔名的組法在 `core.unit` 的 `safeFilename` 測。

## 中文層

上游範本是英文的，內容是軟體專案的示範資料。**逐字直譯沒有用**——那些字使用者本來就要整段
換掉。所以分兩層，都在建置時算好，站上不呼叫 LLM：

1. **公務範例**（`content/zh-samples.json`）：人工把整張圖改寫成公務情境，不是翻譯。
   46 張、16 種圖。深色版與完整版自動沿用標準版那一筆，**但段數不同時就不套**
   （完整版常多畫幾個方塊，硬套會整批錯位）。
2. **骨架字典**（`core.js` 的 `GLOSSARY`）：圖例、月份、季別、是／否、狀態欄位這類
   「換掉內容之後還會留在圖上」的固定用字，141 張都吃得到。

**沒有通用中文譯名的專有名詞一律留原文**（UML、Sankey、Wardley、draw.io、Mermaid、
類別名稱、資料庫欄位名…）。`glossaryLookup` 查不到就回 `null`，原文保留——硬翻更難懂。
`GLOSSARY` 也**不收內容字**（Sprint velocity、Kubernetes、Athena 那些），`core.unit` 有一項盯著。

**中文層是「依序號對應」的**：`extractTextSegments()`（純函式）切出來的段數，必須跟
`editor.js` 的 `collectUnits()`（DOM）切出來的完全一致。兩邊切法一旦不同，中文就整批錯位，
而畫面上只是「字怪怪的」，不會報錯。e2e 逐張比對兩邊的段數，那是這一層唯一的安全網。

畫面上的優先序：**使用者自己改的 → 中文層（開關打開時）→ 範本原文**，
三者共用 `effectiveText(i)`，畫面、文字清單、下載才不會各說各話。

## 九種產生器：這個站的主體

範本庫只能換掉別人排好的字——**方塊是畫死的**，使用者的流程有五步而範本畫四步就沒轍。
用現成範本直接改字就能上的機會太小，所以主路徑改成「填表產生」，範本退成示意與備案。

每一種產生器的 `build(rows, meta, opts)` 收一堆平的物件、吐一整份 SVG。共通紀律：

- **`fields`／`meta` 是規格，`forms.js` 照著長出表單。** 欄位型別只有四種：
  `text`、`select`（要附 `options`）、`check`、`rowref`（下拉，選項來自前面幾列的主要文字）。
  `only: 'decision'` 表示那一格只在型別是判斷時出現。`meta` 收 `text` 與 `select`
  （整張圖共用的設定，例如關係圖的排列方向）。
  加新型別要同步改 `forms.js` 的 `fieldNode()`，`gen.unit` 有一項盯著。
  勾選欄要寫 `hint`（勾了會怎樣），不然使用者得勾了才去圖上找，e2e 有一項盯著。
- **產出的 SVG 用上游那四個色票的字面值**（`#f5f5f5`／`#2d3142`／`#4f5d75`／`#eb6c36`），
  換配色與換字體那兩條路才吃得到它。改成別的顏色會讓自己做的圖換不了色。
- **使用者打的字一律 `escapeXml`**——那是使用者輸入，直接進 SVG 會變成標籤。
- **填錯絕不靜靜少畫一塊**。每一種錯都要進 `warnings`，講得出是第幾列（或哪一項）、
  使用者實際打了什麼、可以怎麼打。顯示在看得見的 `.errbox` 裡。
  **整列全空要安靜跳過**——對著剛按出來的空白列碎唸很煩。
- **一列都沒填時回一張 `emptyCanvas()`**，上面寫「在左邊填什麼」。空白畫面看起來就像壞了。
- **產生器把整張圖當成一張「臨時的範本」交給 `DDEditor.open()`**，
  預覽、換配色、換字體、下載、複製剪貼簿整套沿用，不要再寫一遍。
  每打一個字就重開一次，所以 `open()` 有 `opts.keepTitle`，不然標題會被洗掉。
- **不給逐段改字**（`opts.flow`）。表單是唯一真相；兩個地方都能改字的話，
  使用者改完表單會發現剛才在下面改的字被蓋掉，那更難用。
  設定檔則是**產生器自己一套**（`buildGenProject`／`parseGenProject`，記的是「填了什麼」），
  跟範本那一套（記「第幾段改成什麼」）格式不同、按鈕不同，`kind` 欄位分辨。
- **日期一律吃得下民國年**（`parseTwDate`）：`114/3/5`、`114.3.5`、`114年3月5日`、`1140305`、
  全形數字、西元 `2025-03-05`／`20250305`。年份 ≤ 200 當民國、7 碼當民國、8 碼當西元。

### forms.js 的兩種重畫

- **打字（改欄位內容）→ 只重畫預覽**（debounce 220ms），不要動表單的 DOM，不然游標會跳走。
- **結構改變（新增／刪除／上移下移／改型別／改退回目標）→ 整個表單重建**，
  因為「這一列該出現哪些欄位」「退回目標有哪些選項」會跟著變。

`meta` 那一列的 class 是 `.mrow` 不是 `.frow`——共用的話「有幾列」會多算一列，
e2e 的上移下移會點到那一列去。

### 區塊（`groups`）：一張表分兩段

關係圖與家系圖的一列可以是好幾種東西。全混在一條長表裡時，使用者看到的是
「一列一列長得都不一樣」，第一個問的就是「這是什麼意思」。descriptor 給 `groups`
就分段畫，`forms.js` 照著長：

```js
groups: [
  { id: 'box', title: '方塊', rowName: '方塊', kinds: ['box'], add: '＋ 新增方塊' },
  { id: 'link', title: '連線', rowName: '連線', unit: '條', kinds: ['link'], add: '＋ 新增連線',
    needs: { kinds: ['box'], min: 2, msg: '先填兩個以上的方塊，才能拉連線。' } }
]
```

- **同一區塊的列一定要連在一起**（`sortByGroup`，在 `stateFor()` 收斂一次）。
  下拉選單只列得到「前面幾列」，成員被排到關係後面就選不到了。新增時也要插在
  自己那一段的最後面，不是整份的最後面。
- **只有一種列的區塊不畫型別欄**，多種的只列得到自己那幾種——關係區塊不該還能選回「成員」。
- **鎖要能單獨更新**（`refreshLocks()`）。打字只重畫預覽、不重建表單（重建游標會跳走），
  所以「剛打完第二個成員的名字，＋ 就該亮起來」得靠 `preview()` 順手切按鈕的狀態，
  不能等下一次結構改變。
- **編號從各自那一段數起**（`ordinalIn`）：關係區塊的第一列就寫 1，不是 8。
  上移下移也只在同一段裡動（`sameGroup`）。
- **第一個區塊至少留一列**，不然畫面上只剩兩個標題，看起來像壞了。

### 關係圖：公文附件最常見的那種示意圖

方塊裡寫字、中間拉線、線上再寫一句話。**刻意不做成環狀的「相互關係圖」**——
真實的公務示意圖幾乎都是格狀（四個職域一排、上下兩層），環狀只有在
「每個都跟每個有關」時才好看。排法由「排列方向」「每列（欄）幾個」與各方塊的
「另起一列」決定，程式不猜。

- **直式與橫式只差在把 x／y 對調**：先把方塊分成一條一條「線」（直式一條線是一列、
  橫式一條線是一欄），再依方向擺。不要寫成兩份版面程式，改一邊忘了另一邊是遲早的事。
- **橫式的每一欄要垂直置中**，短的那一欄才不會吊在上面。
- **欄距讓字的判準兩種方向不一樣**：直式的左右鄰居是「同一列」，橫式的左右鄰居是
  「不同欄」。判反了會在不需要的時候把版面撐開。

- **同一列兩個方塊之間有字時，欄距要自動讓開**到放得下那句話。不然「體制內不衡平」
  會擠在 26px 的縫裡，兩邊都壓到框線上。所以欄距是先量標籤、再算方塊寬。
- **「/」是使用者自己決定的斷行點**，其餘照寬度折。`gen.unit` 那一項通用的
  「範例的每一格文字都要出現在圖上」也要跟著把 `/` 拆開比對。
- **雙向箭頭那一頭要另外一個 marker**（`ddg-arrow-back`，`refX` 放在 0 那一端），
  直接沿用 `ddg-arrow` 的話箭頭會凸出線的起點外面。
- **方塊最後畫、線上的字再最後畫**：線可能從方塊旁邊擦過去，先畫方塊會被線壓住；
  字先畫則會被後畫的線劃掉。

### 家系圖：符號慣例就是全部的價值

社工用的 genogram，符號照 McGoldrick 那一套。**畫錯符號等於畫錯意思**，
所以 `gen.unit` 那一段測的是慣例（男方女圓、案主填淡色加雙框、已歿打叉、男左女右、
分居一撇離婚兩撇、收養虛線寄養點線、六種情感關係長得都不一樣），不是版面好不好看。
改動這一支之前先確認慣例，不要憑印象改。

- **一張表填三種東西**：一列可以是成員／伴侶關係／情感關係，用 `kind` 欄分，
  靠既有的 `only:` 機制切換欄位——不必為它改表單引擎。
- **`count` 是「有用到的列數」不是人數**。回人數的話畫面會說「目前 7 個列」，
  但表單裡明明有 13 列。
- **世代由父母算**，父母只能指前面的列（指到後面會繞成圈，畫不出來）。
  沒有父母但有配偶的（嫁進來、娶進來的那一位）跟著配偶的世代走，要多跑幾輪才收斂。
- **有共同子女卻沒填伴侶關係時，要自動補一條線**（標成 `implied`，不寫「結婚」），
  不然子女會憑空垂下來。
- **再婚時「結過兩次的那一位」排中間**，前一段在左、後面的往右接。照填的順序一路往右排的話，
  「他跟第二任」那條線會從第一任頭上跨過去，看起來像第一任也在那段婚姻裡。
- **中間真的卡著人時（三段以上）伴侶線繞到符號上面走**（`unionBlockers`），
  子女線改從空隙垂下（`clearDropX`），而且挑靠近另一半那一側的空隙——挑中間那段會直接
  穿過中間那個人自己的婚姻線，兩條結構線交叉在一起就看不出誰是誰的。
- **情感關係走弧線**（二次貝茲取樣後加法線偏移）。直線會跟結構線混在一起，
  而且更常從別人的符號正中間穿過去。取樣點數跟長度成比例，不然圖例那種短線的鋸齒會糊掉。
- **文字要墊紙色底**（`paperBox`），而且**姓名、註記、婚姻狀態都要最後畫**——
  情感關係的弧線很常從旁邊擦過去，先畫就會被劃掉。
- **`sample` 是可選的**：家系圖在上游那 153 張裡沒有對應的範本，
  沒有就不要在畫面上留一個死連結（`forms.js` 會把整行藏起來）。
- **姓名欄要掛 `only: 'person'`**。不掛的話「伴侶關係」那一列上面會杵著一個填了也沒用的
  姓名欄，整列看起來就像「一個沒有年齡的人」——使用者第一個問的就是這個；
  而且填進去的字會跑進後面幾列的下拉選單，變成一個根本不存在的成員。
  伴侶／情感關係那兩種列的欄位標題也要看得出是「把兩個已填的人連起來」
  （「伴侶」「和哪一位」），不是在新增一個人。
- **同性伴侶要畫得出來**。男左女右只在「一男一女」時套用，同性維持填的順序。
  性別是掛在成員那一列上的，伴侶關係只是指向兩個成員，本來就不需要再問一次性別。
- **範例用稱謂不用姓名**（祖父／父／案主／弟）。家系圖看的是關係，
  寫「陳志明」還要先回頭對一次那是誰。
- **案主填一層淡淡的強調色**（`INDEX_FILL`，透明度 0.18）**再加雙框**。
  社工實務上會把個案塗色，但**塗滿深色的話裡面的年齡與已歿的叉都會被吃掉**
  （做過一版是實心 `C.ink`，字要改成紙色才看得見，整個符號變成一坨黑）。
  淡色底＋雙框，塗色與裡面的資訊兩件事都看得到。`gen.unit` 有一項盯著透明度不要調深。
- **姓名／稱謂預設不畫在圖上**（`meta.names`，預設 `off`）。家系圖是個案資料，
  拿去開會、附在報告裡時姓名多半不能出現；要顯示就改那個選項。
  **表單那邊照樣要填姓名**：父母欄與關係欄的下拉是照姓名長的，只是不畫出來。
  `gen.unit` 版面相關的斷言靠姓名定位，所以 `genoSvg()` 一律把姓名打開，
  「預設不顯示」另外一項專測。
- **學歷是選填的**（`EDU_OPTIONS`，第一個是「不填」）。畫在姓名底下、註記上面，
  三行由上而下：姓名（可關）→ 學歷 → 註記。

### 首頁的縮圖是產生器當場畫的

九個做圖入口的縮圖，是 `gen.build(gen.example, ...)` 真的畫出來的，不是範本、也不是圖片檔。
拿範本當縮圖會騙人：使用者看到別人排的形狀，填完卻拿到另一種東西。

### 流程圖的兩欄版面

`layoutFlow()` 排兩欄：主線在中間（`col: 'main'`），判斷拉出去的分支步驟在右邊（`col: 'side'`）。
分支可以連走好幾步再匯回主線——真實的公文流程幾乎都是這樣，「分岔出去只有一格」畫不出來。

- 分支的第一格跟判斷**同高**（`sideY = 判斷.cy - h/2`），橫線才拉得平。
- 分支結束時主線要**讓到分支底下**（`mainY = max(mainY, sideY)`），匯回線才有地方走。
- 匯回點＝分支之後的第一個主線節點。勾了「分支不回主線」就不畫匯回線，
  最後一格畫成圓角收尾——不然看起來像沒畫完。
- 匯流點是半徑 2.6 的實心圓；**網點底圖也是 circle**，測試要指名半徑才問得準。

### 組織圖的樹狀排版

葉子由左而右一個接一個放（`cursor++`），父節點置中在第一個與最後一個子節點的中點。
這是 tidy tree 的第一課，對編制表這種寬而淺的樹夠用。**上級只能指前面的列**——
指到後面或指到自己會繞成圈，畫不出來，所以那時候當成最上層並講一聲。
整棵樹太寬時整體縮小（`scale`），寧可小一點也不要畫到框外。

### 不會白做工（forms.js）

- **復原**：每一次結構改變前 `snapshot()`，堆疊最多 30 步。刪除、清空、貼上、
  載入設定檔都要進得了復原——「填了二十分鐘按錯一個鍵全沒了」是最容易讓人罵髒話的那種。
- **localStorage**：填的內容寫進 `gongwu-diagram-forms-v1`，純本機、不上傳。
  「資料不離開這台裝置」與「關掉分頁就全沒了」不衝突，後者也是一種資料遺失。
  無痕模式存不進去就**安靜算了**，不要拿這個去煩使用者。
  讀回來時每一格都要照現在的規格驗過——那可能是上一版的站台寫的。

### 版面：預覽一定要看得到

- 寬螢幕（≥1180px）左右兩欄，預覽 `position:sticky` 在右欄。
- 窄螢幕預覽搬到表單**上面**（`.col-b{display:contents}` ＋ `#stageCard{order:0}`）並黏在頂端。
  放在下面的話它永遠在畫面外，「改一格馬上看到」就等於沒有——那是這個工具唯一的賣點。
  下載那一塊不能跟著黏，會擋掉半個畫面，所以用 `display:contents` 把右欄拆開各自排序。
- 手機（≤719px）一列＝一張卡：主要文字獨佔一行、其餘兩兩並排。
  不這樣做的話七個欄位各佔一行，七個工作項目就是一條七千像素的長龍。
  判準是「欄位有沒有指定 `width`」——沒指定的就是主要文字，`.fcell.wide`。

## 畫板：流程圖與關係圖的另一種畫法，定位是「程式先排好，你再微調」

**畫板不是另一種圖**，它就是「自己畫」。所以入口在**流程圖與關係圖**的填表畫面裡
（兩顆按鈕：搬過去、或開空白），**不佔首頁一格**；畫板裡也留一條回填表的路。

- **畫板要記得自己是從哪一種圖來的**（`canvas.js` 的 `origin`／`originId`，跟著形狀一起
  存進 localStorage）。不記的話，關係圖搬過去的那張圖會標成「自己畫流程圖」、
  「回填表畫面」還把人送回流程圖——重開分頁之後最明顯。
  標題、`edKind` 麵包屑、眉標、回去的連結四個地方都吃它。
- **「開空白畫板」也要切 origin**（`setOrigin`），不然從關係圖按進去的空白畫板
  會說自己是流程圖。
- **關係圖 → 畫板走 `GEN.relationBoard()`**，座標沿用預覽算好的那一份。重算一次會差
  幾個像素，使用者會覺得「搬過去圖就跑掉了」。虛線框對到畫板的「註記」、無框對到
  「純文字」，箭頭三種也照樣帶過去。
- **gen.js 與 board.js 互不相依**：`relationBoard()` 回的是純資料，`normBoard()` 再驗一次。

**不是**「另外給一塊空白畫布讓人自己畫」。從空白開始畫流程圖，跟在 Word 裡拉方塊一樣痛苦，
那正是這個站要解決的問題。所以流程圖的填表畫面有一顆「搬到畫板繼續改」，
`boardFromFlow()` 拿 `layoutFlow()` 算好的座標鋪成畫板——切過去就是「一模一樣的那張圖」，
只是每一格都拖得動了。空白畫板留著，但不是主打。

### 連線黏在形狀上（這是畫板存在的前提）

連線記的是 `{from, to}` 的**形狀 id**，不是座標；座標每次重畫時由 `routeLink()` 現算。
記座標的話使用者一拖動就會出現一堆飄在半空的箭頭，而且**不會報錯**。
`board.unit` 與 e2e 各有一項專門盯著「移動形狀之後線的端點有沒有跟著跑」，不要拿掉。

- **菱形的端點要另外算**（`|x|/hw + |y|/hh = 1`）。用外接矩形的話線會停在菱形外面一段距離，
  看起來像沒接上。
- **往回接的線要繞邊走**（`routeAround`）：目標在上面、又跟來源同一欄時，直直往上會穿過
  中間夾著的每一個方塊。巷道挑左右兩邊比較空的那一側。
- **中心只差一點（`JOG`＝30px 以內）就走直線**，不要轉出一個很小的階梯——那看起來像畫錯。
  常見成因是使用者把其中一個方塊拉寬了，中心跟著移了十幾個像素。
- **線上的標籤要畫在所有線之上**（`renderBoard` 分兩輪：先全部的線、再全部的標籤）。
  同一輪畫的話，後畫的線會壓過前一條線的標籤，「否」就變成看不懂的一團。
- **箭頭有三種**（`ARROWS`：單向／雙向／不加）。不是每條關係都有方向——「互相協調」是雙向、
  「同一層級」常常是一條沒有箭頭的線。**沒寫 `arrow` 的一律當成單向**：那是這塊畫板
  一路以來的行為，改成別的會讓舊的設定檔打開之後箭頭整批消失。
  雙向的另一頭要另外一個 marker（`ddb-arrow-back`，`refX` 放在 0 那一端）。
- **連線那一列要 `flex-wrap`**。一列上有五樣東西（對方是誰、線上的字、箭頭、實虛、刪掉），
  不換行的話後面兩顆按鈕會被擠出面板外，點不到——而且畫面上看起來只是「有點擠」。
- **刪掉形狀時連著它的線一起刪**，不然會留下指不到形狀的線（`normBoard` 會丟掉它們，
  但那是最後一道防線，不是正常路徑）。

### 對格線是「拖曳那一刻」的事，不是讀資料時的事

`normShape()` **只驗證與夾範圍，不對格線**。對格線由 canvas.js 在拖曳／縮放／新增時做。
每次讀資料都對的話，從填表搬過來、或載回設定檔時，本來對齊的座標會被推歪幾個像素，
直線就變成一節一節的小階梯——而且完全不會報錯。

### canvas.js 的幾個坑

- **事件要掛在容器 `#bdStage` 上，不能掛在 `<svg>` 上**。`paint()` 每次都把 svg 整個換掉，
  掛在 svg 上的話 `setPointerCapture` 會跟著被消滅——拖曳一動就斷，而且完全看不出哪裡壞了。
- **`pointerdown` 一定要 `preventDefault()`**，不然瀏覽器把「按住拖」當成選取文字，
  整頁反白而且拖不動。`.bdstage` 也要 `user-select:none`。
- **座標換算 x 與 y 都用「寬度的比例」**（SVG 是等比縮放）。用高度算會整個歪掉——
  寫測試時也一樣，這裡踩過一次，看起來像功能壞了其實是測試的座標算錯。
- **加形狀要往下接續排，不要疊在一起**。疊著的話使用者一拖就抓到別的形狀，
  而且看不出剛才加的是哪一個。
- **加形狀用「點一下」不是拖進來**，連線用「點兩下」不是拉小圓點——
  HTML5 drag-and-drop 與邊緣小圓點在觸控裝置上都不能用。

### 單向門

填表 → 畫板可以，畫板 → 填表**不行**（表單記的是「一串有順序的列」，畫板記的是
「一堆有座標的框」）。所以搬過去之前要 `confirm()` 問一聲，而且填表那邊的內容要留著，
使用者隨時可以回去重新產生一次。

`forms.js` 的 `flowNodes()` 跟 `gen.js` 流程圖 `build()` 的前半段是同一套規則
（把列整成節點）。**改了 gen.js 的流程圖欄位，這裡要跟著改。**

## .docx：整張圖換成 Word 原生圖案，打開就能改

**不要再靠 Word 的「轉換成圖形」。** 實測 Word 2019 轉一張家系圖：68 個圖形進去、
51 個圖案出來，**32 段文字一段都沒留**——姓名、年齡、婚姻狀態全不見，只剩一堆線。
（使用者回報時附了轉換後的檔案，拆開來看是 51 個 `v:shape`、`w:t` 只剩頁面上的說明文字。）

所以 `core.js` 的 `svgToWordGroup()` 自己把 SVG 換成 DrawingML 的圖案群組（`wpg:wgp`）：
`rect`→rect／roundRect、`circle`→ellipse、`line`→prst line（用 flipH／flipV）、
`polyline`／`polygon`／`path`→custGeom、`text`→無框無底的文字方塊。`buildDocx()` 先試這條，
換不了才退回原本的「圖片＋SVG＋PNG 後備」。

- **只認得我們自己畫圖用的那七種標籤**。範本庫那 153 張是別人寫的任意 SVG
  （CSS class、漸層、巢狀 svg、foreignObject 都有），一律回 `null` 退回圖片——
  猜著轉會產出一張缺東少西的圖，比一張不能編輯的圖更糟。
- **子座標系直接用最終 EMU**（`chExt` == `ext`，縮放比 1）。用「SVG 單位 × 100」那種
  中介座標系的話，群組被縮放時**文字不會跟著縮**，字級要另外算，很容易對不上。
- **屬性名字裡有數字**（`x1`、`y2`…）。字元類別寫成 `[a-zA-Z-]+` 的話，整條線的座標
  會全部讀成 0，圖上所有直線一次消失——而且完全不報錯。踩過一次。
- **巢狀 `<svg>` 要遞迴進去**：範本那條路與 `composeExportSvg()` 都會生出巢狀 svg，
  不處理的話整張圖都換不成圖案。多行標題是 `<tspan dy>` 疊的，一個 tspan 做一個文字方塊。
- **.docx 裡只有圖本身**：`saveDocx()` 傳的是 `currentSvgMarkup()`（原圖），不是
  `composeExportSvg()` 組的那張。眉標、標題、說明、底色都不進 Word——使用者要的是
  可以直接貼進公文的圖，標題他自己會下。整張紙面的底色矩形也在 `walkSvgShapes()` 裡
  略掉（`rw >= W*0.99 && rh >= H*0.99`），不然解除群組後第一個點到的永遠是那張底圖。
  只有「從範本來的」才會多一段來源標註（`credit`），自己填表產生的圖不加。
- **字要夠大：整張一起放大，不逐段設下限**。原圖的字級是 8～10px（≈6～7.5pt），
  在 Word 裡小到不能看。做法是先跑一趟量測（`pass(1,0,0)`）拿到最大字級，再算
  `scale = max(1, min(3, 14 / (最大字 × 0.75)))`，第二趟整張圖照這個倍率重畫。
  **關鍵是不要逐段拉到 14pt**：那樣標題跟註記會變成一樣大，圖的層次就沒了。
  現在是最大的那一段剛好落在 `MIN_PT`（14），其餘按原比例跟著長（實測家系圖
  9.5／10／14pt、流程圖 8.5～14pt）。上限 `WORD_MAX_SCALE`（3）是免得一張本來就
  大字的圖被放到超出紙面。
- **文字方塊要 `wrap="none"`、`anchor="ctr"`、四邊內距 0、`<a:spAutoFit/>`**，寬度按
  「中文一個字寬、英數 0.55」估。`spAutoFit` 是使用者回報來的：用 `noAutofit` 時，
  在 Word 裡把字調大會被文字方塊原本的大小擋住，字直接被切掉。非文字的圖案仍是
  `noAutofit`（`core.unit` 那項斷言只挑 `<wps:txbx>` 的區塊看，不要改成整份檔案找）。
- **頁面跟著圖走**，不是圖去遷就 A4：`pgSz` 用圖的尺寸加 1cm 邊界，寬>高就補
  `w:orient="landscape"`。放大三倍後的圖塞進直式 A4 只會被裁掉一半。
- ZIP 只做 store（不壓縮）。docx 允許，而且省掉一整包 deflate——零相依是鐵律。
  時間固定成 1980-01-01，同樣的內容壓出同樣的位元組，測得起來。
- **CI 驗不了 Word 的實際行為**（容器裡的 LibreOffice 連純文字檔都載不進去，實測過）。
  能驗的是：封裝是合法的 OOXML、每一段字都變成了文字方塊、座標與原圖對得上、
  最大字級剛好 14pt 而且字級不只一種、頁面尺寸跟著圖走、圖以外沒有多餘的段落。
  座標那一項是把產出的圖案**倒著畫回 SVG** 跟原圖並排比對出來的
  （`tests/` 之外的一次性工具，做法記在這裡：拆 `wps:wsp` 的 `a:off`／`a:ext`／
  `a:custGeom` 再畫成 SVG）。改動這一塊之後，還是要拿真的 Word 開一次。

## 產品取捨：焦點是「Excel 做不到的圖」

使用者的痛點是**流程圖超難做**；長條圖、折線圖、圓餅圖 Excel 兩下就有。
所以九種產生器全部是 Excel 做不到的那類；範本清單預設也只給 `COMMON_TYPES`——
那份清單的判準是「Excel 做不到、自己排會排到崩潰」，**不是**「公務常見」。
長條圖與折線圖仍留在庫裡（有人就是想在這裡做各種事），只是不放在預設的視野裡。

`core.js` 的 `STARTERS`（七張範本快捷入口）退到範本區裡。改它時記得：
`tests/data.mjs` 會檢查每一張都存在且已寫好整份中文——點下去卻是滿滿英文，比沒有更糟。

## 幾個後來加的省事功能

- **複製圖片到剪貼簿**：`navigator.clipboard.write` ＋ `ClipboardItem`。
  需要安全內容（https 或 localhost），不支援時要明講「請改按下載 PNG」，不要靜靜失敗。
- **從 Excel 貼一整欄**：一行對一段，從「目前選取的那一段」開始依序填。
  一行裡有多欄（tab）併成「甲 · 乙」，**不拆成兩段**——拆了後面每一段都會錯位。
- **存出／載入設定檔**：存的是「使用者改了什麼」，不是整張 SVG，範本更新後舊設定照樣套得上。
  `parseProjectFile` 的每一種失敗都要講人話，不能讓使用者看到 `JSON.parse` 的英文錯誤；
  設定檔是使用者給的輸入，序號 key 要驗過再用。
- **整張換字體**：`forceFontFamily()`，公文常用標楷體。一樣只寫字族名，
  沒裝標楷體的機器要退回襯線體才不會變成預設字。

## 修改紀律

1. 行為改了，就要同步改畫面上的 `hint` 說明文字與 `README.md`。
2. 信任邊界的輸入驗證、防資料遺失的錯誤處理、無障礙屬性不可省
   （e2e 有一組專測 label／`role=alert`／`aria-live`）。
3. 錯誤訊息一律講人話、講得出下一步該做什麼（例：「這張圖轉不成 PNG，請改用 SVG 或 HTML 下載」）。
4. **預覽框是唯一真相**：使用者在畫面上看到什麼，下載的檔案就是什麼。
   不要在旁邊偷偷維護一份看不見的結構。產生器則是「表單是唯一真相」，同一個道理。
5. 使用者的真實需求是「把英文範本改成中文」，而中文比英文寬。改任何跟字級、折行、
   框線有關的東西之前，先拿一張真的塞滿中文的圖看過。

## 測試（推上去前必做，沒有例外）

| 指令 | 內容 |
|---|---|
| `npm test` | 全部：`core.unit`（166）＋`gen.unit`（104）＋`board.unit`（38）＋`data`（25）＋`e2e`（125），約 4 分鐘 |
| `npm run test:unit` | 純函式＋資料一致性，約 3 秒 |
| `npm run test:e2e` | 只跑瀏覽器實測 |

- 單元測試直接 `require` 站上那份 `app/core.js`、`app/gen.js`、`app/board.js`，**不做副本**。
- `gen.unit` 測「填什麼就得到什麼」與「填錯時有沒有講人話」，**不測座標**——
  座標調版面時本來就會動，寫死了只會讓人不敢改版面。
- `tests/data.mjs` 會重跑一次 `scripts/build-data.mjs` 並跟 commit 進來的 JSON 逐字比對，
  這是唯一擋得住「建置產物走鐘」的關卡。
- e2e 逐一開完 153 張範本，確認每一張都畫得出來、文字清單都對得上，
  並比對純函式與 DOM 兩邊切出來的段數（中文層的安全網，見上）。
  新增或移除範本時，張數寫死在 `tests/e2e.mjs`（多處）與 `index.html` 的說明文字裡；
  中文範本的張數也寫在 `index.html` 的說明卡與 `README.md`。
- 錯誤訊息的測試要斷言**看得見**（`isVisible()`），不能只斷言 `textContent`。
  **收在闔上的 `<details>` 裡也等於看不見**——首頁的 `#galleryErr` 就是因此刻意放在收合區外面。
- 不能用 `file://` 開來測，`fetch()` 會被擋。
- 量版面時**捲動之後不能用視窗座標比先後**：黏住的那一塊 top 是 50，捲過去的表單則是負的。
  要回到頁頂用文件座標量。

## 部署

**push 到 main 就上線。** 兩條路二選一，細節與後台設定見 `README.md` 的「部署」段：

- **(a) Cloudflare 後台 Git 整合**：建置指令 `npm run dist`、輸出目錄 `dist`，不必弄 token。
  這時 `.github/workflows/deploy.yml` 仍會跑測試，但**部署那幾步自己跳過**（`steps.creds.outputs.ok`）
  ——沒憑證時跳過而不是失敗，每次推 main 都亮紅叉的話久了就沒人看 Actions 了。
- **(b) GitHub Actions 部署**：要 repo secret `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`
  （每個 repo 各自獨立）。**兩條路不要同時開**，會互相蓋掉部署。
- 部署包由 `scripts/dist.mjs` 組（`npm run dist`），workflow 與 Cloudflare 後台**共用同一份清單**。
  兩邊各寫一份的話遲早會一邊上線、另一邊少檔案。
  **新增 `app/*.js` 時要同步加進 `REQUIRED`**，不然首頁看起來完全正常，
  使用者一點做圖入口才發現整支壞掉。
- Pages 專案的 production branch 寫死 `main`，不要用 `default_branch`。
- 網址一律從 Actions 的部署摘要讀，不要猜——Pages 子網域撞名會自動加後綴。
- 驗收不能只看首頁：要進一支產生器改一格字、加一列、下載一次 PNG 與一次 Word 檔
  （**Word 檔要真的用 Word 開一次**：確認整張圖是一個群組、右鍵可以「取消群組」、
  取消後每個方塊每段字都點得到，而且把字調大不會被文字方塊切掉——這些 CI 驗不了）；
  再進畫板拖一個方塊、連一條線，確認線黏著；範本那條路也點一張。

## 跟工具箱的關係

這是「公務用工具箱」（gongwu-calc）的衛星站台，入口卡片掛在工具箱的 `index.html`
（`diagramTemplateTool()`）。更新這支工具＝改這個 repo，工具箱的 index.html 不用動；
只有網址變了才要回去改那張卡片。
